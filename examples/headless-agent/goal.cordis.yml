# Add the persisted goal domain and its model-facing tools to the real one-shot app.
- id: base
  name: '@relay-harness/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: goal
            name: '@relay-harness/rlh-goal'
          - id: tool-goal
            name: '@relay-harness/rlh-tool-goal'
