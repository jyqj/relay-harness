/** URLs represented by real Markdown link, image, or definition nodes. */
export function markdownLinkTargets(source: string): string[]
/** Execute the complete repository-governance and feature-status check. */
export function main(): Promise<void>
