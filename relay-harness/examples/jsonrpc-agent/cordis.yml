# Unattended coding-agent deployment for the bundled rlh-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@relay-harness/rlh-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.RLH_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.RLH_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@relay-harness/rlh-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@relay-harness/rlh-subprocess-local'

- id: bash
  name: '@relay-harness/rlh-bash-local'
  config:
    cwd: !!js process.env.RLH_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@relay-harness/rlh-agent-spine-demo'
  config:
    persona: !!js process.env.RLH_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@relay-harness/rlh-session-persistence-jsonl'
  config:
    root: !!js process.env.RLH_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.RLH_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@relay-harness/rlh-session-checkpoint-policy'

- id: subagent
  name: '@relay-harness/rlh-subagent'

- id: subagent-spawn-in-process
  name: '@relay-harness/rlh-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@relay-harness/rlh-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@relay-harness/rlh-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@relay-harness/rlh-fs-local'
  config:
    cwd: !!js process.env.RLH_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@relay-harness/rlh-fs-observation-policy'

- id: tool-fs
  name: '@relay-harness/rlh-tool-fs'

- id: token-meter
  name: '@relay-harness/rlh-token-meter'

- id: compaction-basic
  name: '@relay-harness/rlh-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
