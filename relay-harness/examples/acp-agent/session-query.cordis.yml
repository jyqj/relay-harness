# Explicit session-query tool opt-in for the dedicated spill scenario. The
# nested filesystem overlay supplies private spill storage and its byte limit.
- id: base
  name: '@relay-harness/cordis-plugin-include'
  config:
    path: ./fs.cordis.yml
    patches:
      - insert:
          - id: tool-session-query
            name: '@relay-harness/rlh-tool-session-query'
          - id: timeout-policy
            name: '@relay-harness/rlh-tool-call-timeout-policy'
