# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@relay-harness/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@relay-harness/rlh-cordis-host-runner'
          - id: tool-cordis
            name: '@relay-harness/rlh-tool-cordis'
