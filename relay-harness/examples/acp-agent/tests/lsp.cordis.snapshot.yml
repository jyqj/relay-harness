# Keyless replay keeps the LSP composition intact and replaces only the model adapter.
- id: base
  name: '@relay-harness/cordis-plugin-include'
  config:
    path: ../cordis.yml
    patches:
      - id: llm-deepseek
        name: '@relay-harness/rlh-llm-deepseek'
        disabled: true
      - insert:
          - id: lsp
            name: '@relay-harness/rlh-lsp'
          - id: lsp-stdio
            name: '@relay-harness/rlh-lsp-stdio'
            config:
              servers:
                fixture:
                  command: !!js process.execPath
                  args: ['./lsp-server.mjs']
                  extensionToLanguage:
                    '.ts': typescript
          - id: timeout-policy
            name: '@relay-harness/rlh-tool-call-timeout-policy'
          - id: tool-lsp
            name: '@relay-harness/rlh-tool-lsp'
            config:
              maxLocations: 1
          - id: llm-replay
            name: '@relay-harness/rlh-llm-replay'
