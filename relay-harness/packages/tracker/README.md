# Tracker packages

English | [中文](README.zh.md)

The tracker group defines provider-neutral issue identity, scheduling reads, and session-bound provider-native tools. `tracker` owns the effect-scoped registry and capture validation; `tracker-linear` is the first complete Provider.
