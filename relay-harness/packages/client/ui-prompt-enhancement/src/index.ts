/**
 * Prompt Enhancement UI plugin, node half. The browser half registers an
 * Enhance control through the conversation input slot.
 */

/** Host plugin body; browser behavior ships through `./client`. */
export function apply(): void {}
