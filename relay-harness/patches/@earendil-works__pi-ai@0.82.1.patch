diff --git a/dist/api/openai-completions.js b/dist/api/openai-completions.js
index ed81baa3860fa20cf9d38885f9dd4b6b2f639045..3deae3db1cc47512620d8621263283dce2c7b2fe 100644
--- a/dist/api/openai-completions.js
+++ b/dist/api/openai-completions.js
@@ -143,6 +143,18 @@ export const stream = (model, context, options) => {
             });
             await options?.onResponse?.({ status: response.status, headers: headersToRecord(response.headers) }, model);
             stream.push({ type: "start", partial: output });
+            // OpenAI's request-level signal can end iteration without aborting the
+            // Stream's body-owning controller after response headers arrive. Link
+            // the caller signal to that controller so cancellation closes the socket.
+            const abortOpenAIStream = () => {
+                openaiStream.controller.abort();
+            };
+            if (options?.signal?.aborted) {
+                abortOpenAIStream();
+            }
+            else {
+                options?.signal?.addEventListener("abort", abortOpenAIStream, { once: true });
+            }
             let textBlock = null;
             let thinkingBlock = null;
             let hasFinishReason = false;
@@ -305,7 +317,8 @@ export const stream = (model, context, options) => {
                 applyPendingReasoningDetail(block);
                 return block;
             };
-            for await (const chunk of openaiStream) {
+            try {
+                for await (const chunk of openaiStream) {
                 if (!chunk || typeof chunk !== "object")
                     continue;
                 // OpenAI documents ChatCompletionChunk.id as the unique chat completion identifier,
@@ -421,6 +434,10 @@ export const stream = (model, context, options) => {
                         }
                     }
                 }
+                }
+            }
+            finally {
+                options?.signal?.removeEventListener("abort", abortOpenAIStream);
             }
             for (const block of blocks) {
                 finishBlock(block);
