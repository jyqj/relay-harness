# Room parent: sequential member dispatch is owned by the dshbot plugin
# llm/stream listener. This composition only restricts the room agent to
# the plugin's ask_participant tool.

- id: persona
  name: '@deepseek-ai/dsh-persona'
  config:
    includeRuntimeContext: false
    complete: true
    text: ''

- id: ask-participant
  name: dshbot/ask-participant
