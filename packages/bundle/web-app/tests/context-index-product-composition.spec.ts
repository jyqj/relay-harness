import { readFileSync }from'node:fs';import{ resolve }from'node:path';import{ fileURLToPath }from'node:url';import{ describe,expect,it }from'vitest';import*as yaml from'js-yaml';import{ entryListSchema }from'@relay-harness/cordis-plugin-include'
describe('Web Context and Code Index product composition',()=>{it('ships provider, contributor, projection, Host Remote and both Client surfaces',()=>{const root=fileURLToPath(new URL('..',import.meta.url));const manifest=JSON.parse(readFileSync(resolve(root,'package.json'),'utf8'))as{ dependencies:Record<string,string> };const parsed=yaml.load(readFileSync(resolve(root,'cordis.patch.yml'),'utf8'),{ schema:entryListSchema })as{ insert?:{ id:string;name:string }[] }[];const rows=parsed.flatMap(item=>item.insert??[]);for(const[id,name]of[['code-index-workspace-router','@relay-harness/rlh-code-index-workspace-router'],['code-context','@relay-harness/rlh-code-context'],['context-inspector','@relay-harness/rlh-context-inspector'],['code-index-center','@relay-harness/rlh-host-code-index-center'],['ui-context-inspector','@relay-harness/rlh-client-ui-context-inspector'],['ui-code-index-center','@relay-harness/rlh-client-ui-code-index-center']]as const){expect(rows.find(row=>row.id===id)).toMatchObject({ name });expect(manifest.dependencies).toHaveProperty(name)}})})
