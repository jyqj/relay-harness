/**
 * @relay-harness/rlh-base — the shared rlh core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `rlh.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @relay-harness/rlh-base
 */

export {}
