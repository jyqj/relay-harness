/** Browser plugin owning the local URL preview surface. */

export { apply, inject } from './apply.ts'
export type {
  PreviewBounds, PreviewKey, PreviewPanelProps, PreviewResult, PreviewShellInjected,
} from './apply.ts'
